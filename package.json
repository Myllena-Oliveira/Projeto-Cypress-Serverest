{
  "name": "projeto-cypress",
  "version": "1.0.0",
  "scripts": {
    "test:e2e": "cypress run --spec 'cypress/e2e/frontend/*'",
    "test:api": "cypress run --spec 'cypress/e2e/api/*'",
    "test:bdd": "cypress run --spec 'cypress/e2e/bdd/features/**/*.feature'"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/e2e/bdd/step_definitions/**/*.{js,ts}"
  },
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^24.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.8",
    "@shelex/cypress-allure-plugin": "^2.41.2",
    "allure-commandline": "^2.34.1",
    "cypress": "^13.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
